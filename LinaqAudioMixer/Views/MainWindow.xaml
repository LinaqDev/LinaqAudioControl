<Window x:Class="LinaqAudioControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LinaqAudioControl"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" local:StorePositionBehavior.StorageKey="MainWindowPosition">
    <Grid>
        <ItemsControl ItemsSource="{Binding SoundDevices}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <Label Content="{Binding Name}"/>
                        <ProgressBar Value="{Binding CurrentVolume, Mode=OneWay}" Maximum="1" Height="5" Margin="3"/>
                        <Button Content="+" Command="{Binding DataContext.VolumeUpCmd, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}"/>
                        <Button Content="-" Command="{Binding DataContext.VolumeDownCmd, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}"/>
                    </StackPanel> 
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Window>
